#include <stdio.h>
#include <stdlib.h>

int main()
{
   /* FILE* miArchivo;

    int* x = (int*) malloc(sizeof(int));

    *x = 4;

    miArchivo = fopen("DatoBinarios", "wb");//va sin extension, porque la extension escomo el sistema lo reconoce pero lo importante es como lo leo y como lo escribo.
    //por lo general, para abrir un archivo de datos, se usa .DAT pero podria no estar la extension.

    fwrite(x, sizeof(int), 1, miArchivo);

    fclose(miArchivo);*/

    /*EJERCICIO 3:

    FILE* miArchivo;

    int* x;

    x = (int*) malloc(sizeof(int));

    if(miArchivo != NULL)
    {
        if(miArchivo = fopen("DatosBinarios", "rb"))
        {
            if(fread(x,sizeof(int),1,miArchivo))
            {

            }
        }
    }

    fclose(miArchivo);

    printf("%d", *x);*/

    typedef struct
    {
        int legajo;
    }


    return 0;
}
