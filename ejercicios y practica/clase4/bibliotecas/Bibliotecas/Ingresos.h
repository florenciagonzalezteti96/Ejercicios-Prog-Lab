/*#include "Ingresos.h" --> solo se agrega en el .c porque si no crea una referencia cruzada, cae el programa
seimpre se docuemnta arriba de las funciones
*/

/*permite que el usuario ingrese un numero * \
 *
 * \un texto y la variable que se pide. El mensje a ser mostrado (que representa el parametro que tiene la funcion)
 * \ un entero. El numero ingresado.
 *
 */

/** \brief
 *
 * \param
 * \param
 * \return
 *
 */

int pedirEntero (char []);// pide y devuelve un entero, para personalizar el printf dependiendo de la variable.

